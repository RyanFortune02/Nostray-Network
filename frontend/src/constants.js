/*
Things that a constants file might include are:
Authentication configuration (token lifetimes, secret keys)
User role definitions
API endpoints
Error messages
HTTP status codes
Validation parameters (password requirements, etc.)
*/
export const ACCESS_TOKEN = 'access';
export const REFRESH_TOKEN = 'refresh';
export const USER_ID = 'user_id';